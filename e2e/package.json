{
  "name": "bytebucket-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright browser E2E for the ByteBucket admin panel. Isolated from web/ so the production image build never pulls Playwright or its browsers.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
